"""Neural-network architectures for GridForge.

PyTorch is an optional dependency; importing the submodules here will
raise ImportError if torch is not installed. Install with:

    pip install 'gridforge[ml]'
"""
